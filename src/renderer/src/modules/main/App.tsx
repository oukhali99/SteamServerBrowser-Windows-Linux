import React from "react";

import { ServersRoot } from "@renderer/modules/servers";

const App = () => {
    return (
        <div>
            <ServersRoot />
        </div>
    );
};

export default App;
