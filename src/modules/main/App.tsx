import React from "react";

import { ServersRoot } from "modules/servers";

const App = () => {
    return (
        <div>
            <ServersRoot />
        </div>
    );
};

export default App;
